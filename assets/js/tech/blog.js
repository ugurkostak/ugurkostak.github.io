// Tech blog wrapper (placeholder)
// Delegates to unified blog.js if present
(function(){
  if (window.UgurTechBlog) return;
  // If unified blog.js exposes an init, call it (no-op fallback)
  if (typeof window.masonryBuild === 'function') {
    // placeholder
  }
})();
